uvicorn[standard]==0.40.0
fastapi==0.128.0
pydantic==2.12.4
pydantic-core==2.41.5
fastexcel>=0.11.0
pandas>=2.1.0
numpy
python-dateutil==2.9.0.post0

openpyxl==3.1.5
xlsxwriter==3.2.9

python-multipart==0.0.20
python-dotenv

requests==2.32.5
httpx
psutil

polars
pyarrow
duckdb>=1.0.0

# GenAI / Gemini (optional - only required if using GenAI features)
google-genai

# Optional: low-level Excel helpers
python-calamine==0.6.1

# Local semantic column matching — Gemini fallback (sentence-transformers)
# model all-MiniLM-L6-v2 (~80 MB) is downloaded on first use from Hugging Face
sentence-transformers